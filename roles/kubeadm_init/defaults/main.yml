---
k8s_control_plane_node_name: "{{ inventory_hostname }}"
k8s_kubeconfig_dir: "{{ k8s_user_home }}/.kube"
k8s_kubeconfig_path: "{{ k8s_kubeconfig_dir }}/config"

k8s_extra_python_packages:
  - python3-pip
  - python3-openshift
  - python3-yaml
  - python3-kubernetes
